---
author: mdo
date: "2026-08-21T00:00:00Z"
title: "Bootstrap 6: New & Improved Components"
category: Release
description: New and rebuilt Bootstrap 6 components, from generative variants to native dialog, drawer, accordion, and carousel.
keywords:
  - bootstrap
  - release
draft: true
---

import Example from '../../../components/shortcodes/Example.astro'
import ResizableExample from '../../../components/shortcodes/ResizableExample.astro'
import ComponentWall from '../../../components/shortcodes/ComponentWall.astro'
import PostSeries from '../../../components/shortcodes/PostSeries.astro'
import { themeColors, themeColorTitle } from '../../../libs/theme-colors'

For our third post in the Bootstrap 6 series, we're taking a look at all our new and improved components. We'll cover everything from new aesthetics, generative variants, the use of more native HTML elements, some new CSS strategies, and various dependencies.

Heads up though, we won't be covering any form components here yet—those will come later.

<PostSeries current="components" />

## What’s new

Honestly, nearly every component has been touched in some way, and many of them have been completely overhauled to implement new standards and features, update dependencies, and restyle for a more modern look and feel. Here's a little taste to start.

<ComponentWall />

Brand new components, or completely rewritten components, in v6 include:

- **Avatar** with sizes, status indicators, and `.avatar-stack` for grouped avatars
- **Chip** for tags and tokens (along with a Chip Input that we'll cover in the upcoming forms post).
- **Dialog** replaces Modal with native `<dialog>` element (including its native `showModal()`, `show()`, and `close()` APIs), plus new transition effects and styles.
- **Drawer**, like Dialog, replaces Offcanvas and is built on a shared architecture with Dialog.
- **Menu** replaces Dropdown with Floating UI for placement, submenu support, new styles, new options, and more.
- **Nav overflow**, a wrapper that collapses whatever doesn’t fit in a wide navigation into an automatic overflow menu.
- **Prose** for scoping rich typography across long-form content.
- **Stepper** for multi-step workflows, CSS-only
- **Toggler**, a small plugin for toggling classes or attributes via `data-bs-toggle="toggler"`

Other components that have been significantly revamped in v6 include:

- **Accordion** was rebuilt with native `<details>` and `<summary>` elements, plus updated styles and options.
- **Badge** has been refined with new styles, plus an outline and subtle option with theme variants.
- **Breadcrumb** has new styles and divider options.
- **Button** has new base class styles, three new variants, new extra small and icon-only sizes, and more.
- **Card** has updated styles, namely around `border-radius` management and flexbox layout.
- **Carousel** was rebuilt on CSS scroll snap, prioritizing the modern content carousel paradigms over the traditional image-based slideshow. It also features revamped autoplay behavior, new controls, more flexible layout, multiple items at once, new end behaviors, and more.
- **Close button** was rebuilt to use CSS masks instead of SVGs, plus updated styles and better dark mode support.
- **Navbar** was rebuilt to use container queries and the Drawer component for its responsive behavior, plus a few other style updates.
- **Scrollspy** has a new detection strategy that's more robust and accurate.
- **Tooltip** and **Popover** have been rewritten to use Floating UI for their placement and are now always available (unlike in v5, where they were opt-in due to performance concerns).

We won't go into every change of every component, but our [migration guide](https://getbootstrap.com/docs/6.0/guides/migration) has an exhaustive list of all the changes (and how to migrate from v5 to v6) if you want to dig in. Instead, let's jump into spotlighting some of our favorite component changes.

## Buttons

Buttons just might be one of the most powerful Sass components in Bootstrap 6. Previously, we had few style variants for buttons—enable a global Sass variable for gradients and switch between default and outline styles. New in v6, we have [four style variants](https://getbootstrap.com/docs/6.0/components/button/#variants) plus a unique [styled hybrid modifier](https://getbootstrap.com/docs/6.0/components/button/#styled-buttons), [four sizes](https://getbootstrap.com/docs/6.0/components/button/#sizes), and an [icon-only option](https://getbootstrap.com/docs/6.0/components/button/#icon-buttons).

<Example full toggleMarkup class="bd-example-buttons md:justify-items-start" code={themeColors.map(color => `<button type="button" class="btn-solid btn-styled theme-${color}">${themeColorTitle(color)}</button>
<button type="button" class="btn-solid theme-${color}">${themeColorTitle(color)}</button>
<button type="button" class="btn-outline theme-${color}">${themeColorTitle(color)}</button>
<button type="button" class="btn-subtle theme-${color}">${themeColorTitle(color)}</button>
<button type="button" class="btn-text theme-${color}">${themeColorTitle(color)}</button>`)} />

Building those variants with our new [theme system](/2026/08/21/bootstrap-6-colors/) took some work, but what we landed on feels super powerful for folks to customize—a series of interconnected Sass maps and mixins that work together to generate styles for every variant.

Like all other components, `_button.scss` starts with a `$button-tokens` Sass map for all the CSS variables we use. There are also separate `$button-link-tokens` and `$button-styled-tokens` maps for the link and styled variants. Button sizes are also a Sass map, `$button-sizes`, that works in conjunction with the variants map to generate styles for each size.

```scss
// Imports come first…

// Token maps next…
// Not shown here: $button-link-tokens and $button-styled-tokens
$button-tokens: () !default;s
$button-tokens: defaults(
  (
    // …
  ),
  $button-tokens
);

// Button sizes…
$button-sizes: () !default;
$button-sizes: defaults(
  ("xs", "sm", "lg"),
  $button-sizes
);
```

The variants map, `$button-variants`, is a more complex Sass map that defines each variant at the top level, then assigns theme token names to CSS properties for each button state. So for `.btn-solid`, the `background` and `border-color` use the same theme `bg` token. Each variant get's the manual mapping, meaning you can customize, remove, or add variants as desired.

```scss
$button-variants: defaults(
  (
    "solid": (
      "base": (
        "bg": "bg",
        "color": "contrast",
        "border-color": "bg"
      ),
      "hover": (
        "bg": "bg",
        "border-color": "bg",
        "color": "contrast"
      ),
      "active": (
        "bg": "bg",
        "border-color": "bg",
        "color": "contrast"
      )
    ),
    "outline": (
      "base": (
        "bg": "transparent",
        "color": "fg",
        "border-color": "border"
      ),
      "hover": (
        "bg": "bg",
        "color": "contrast",
        "border-color": "bg"
      ),
      "active": (
        "bg": "bg",
        "color": "contrast",
        "border-color": "bg"
      )
    ),
    // Plus 2 other variants…
  ),
  $button-variants
);
```

From the variants map, we generate a selector list needed for a shared ruleset for all the variants. That ruleset gets the tokens, styles, and more. The selector list lets us get away without needing a base class on every instance of a button in your HTML.

```scss
$btn-variant-selectors: (string.unquote(".btn"), string.unquote(".btn-link"), string.unquote(".btn-icon")) !default;
@each $variant, $config in $button-variants {
  $btn-variant-selectors: list.append($btn-variant-selectors, string.unquote(".btn-#{$variant}"), comma);
}
```

The output of that looks like this in our compiled CSS:
```css
.btn,
.btn-link,
.btn-icon,
.btn-solid,
.btn-outline,
.btn-subtle,
.btn-text {
  /* Tokens and styles… */
}
```

Then finally, the loop does all the heavy lifting and generates the styles for all our variants.

```scss
@layer components {
  #{$btn-variant-selectors} {
    @include tokens($button-tokens);

    // Shared base styles…
  }

  @each $variant, $config in $button-variants {
    .btn-#{$variant} {
      @each $property, $value in map.get($button-variants, $variant, "base") {
        @if $value == "transparent" {
          --btn-#{$property}: transparent;
        } @else {
          --btn-#{$property}: var(--theme-#{$value});
        }
      }
      // Plus other variant-specific styles…
    }
  }
  // Rest of the stylesheet…
}
```

Oh, and one more button thing—did you notice the `.btn-group-divider` option? Trendy, perhaps, but it's also a lovely way to provide some affordance without sacrificing aesthetics.

<Example full toggleMarkup class="d-flex justify-content-center" code={`<div class="btn-group btn-group-divider theme-primary">
  <button class="btn-solid">Button 1</button>
  <button class="btn-solid">Button 2</button>
  <button class="btn-solid">Button 3</button>
</div>`} />

## Carousel

Rebuilding the carousel on scroll snap handed us a pile of features we didn’t have to write. Sliding, touch dragging, momentum, and keyboard scrolling are all just native scrolling now. On top of that we can show multiple slides at once, reveal a peek of the adjacent ones, add gaps, center the active slide, and support variable-width slides—all through CSS custom properties.

<Example full toggleMarkup code={`<div id="carouselStackedIndicators" class="carousel slide">
    <div class="d-flex justify-content-between align-items-center">
      <div>
        <button class="btn-icon btn-sm" type="button" data-bs-target="#carouselStackedIndicators" data-bs-slide="prev">
          <span class="carousel-icon-prev" aria-hidden="true"></span>
          <span class="visually-hidden">Previous</span>
        </button>
        <button class="btn-icon btn-sm" type="button" data-bs-target="#carouselStackedIndicators" data-bs-slide="next">
          <span class="carousel-icon-next" aria-hidden="true"></span>
          <span class="visually-hidden">Next</span>
        </button>
      </div>
      <div class="carousel-indicators">
        <button type="button" data-bs-target="#carouselStackedIndicators" data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
        <button type="button" data-bs-target="#carouselStackedIndicators" data-bs-slide-to="1" aria-label="Slide 2"></button>
        <button type="button" data-bs-target="#carouselStackedIndicators" data-bs-slide-to="2" aria-label="Slide 3"></button>
      </div>
    </div>

    <div class="carousel-inner rounded-5">
      <div class="carousel-item active">
        <Placeholder width="800" height="300" class="bd-placeholder-img-lg d-block w-100" color="#555" background="#777" text="First slide" />
      </div>
      <div class="carousel-item">
        <Placeholder width="800" height="300" class="bd-placeholder-img-lg d-block w-100" color="#444" background="#666" text="Second slide" />
      </div>
      <div class="carousel-item">
        <Placeholder width="800" height="300" class="bd-placeholder-img-lg d-block w-100" color="#333" background="#555" text="Third slide" />
      </div>
    </div>
  </div>`} />

Set `--bs-carousel-items` to show several slides at once. Combine it with `--bs-carousel-items-gap` for spacing.

<Example full toggleMarkup code={`<div id="carouselMultiple" class="carousel slide" data-bs-ends="stop" style="--bs-carousel-items: 3; --bs-carousel-items-gap: 1rem;"><!--[!code highlight]-->
    <div class="d-flex justify-content-between align-items-center">
      <h5 class="mb-0">Three at a time</h5>
      <div>
        <button class="btn-icon btn-sm" type="button" data-bs-target="#carouselMultiple" data-bs-slide="prev" style="--bs-carousel-control-icon-width: 1rem;">
          <span class="carousel-icon-prev" aria-hidden="true"></span>
          <span class="visually-hidden">Previous</span>
        </button>
        <button class="btn-icon btn-sm" type="button" data-bs-target="#carouselMultiple" data-bs-slide="next" style="--bs-carousel-control-icon-width: 1rem;">
          <span class="carousel-icon-next" aria-hidden="true"></span>
          <span class="visually-hidden">Next</span>
        </button>
      </div>
    </div>
    <div class="carousel-inner">
      <div class="carousel-item active"><Placeholder style="width: 260px" class="bd-placeholder-img d-block h-auto ratio-1x1 rounded-5" color="#fff" background="#777" text="1" /></div>
      <div class="carousel-item"><Placeholder style="width: 260px" class="bd-placeholder-img d-block h-auto ratio-1x1 rounded-5" color="#fff" background="#6c6c6c" text="2" /></div>
      <div class="carousel-item"><Placeholder style="width: 260px" class="bd-placeholder-img d-block h-auto ratio-1x1 rounded-5" color="#fff" background="#666" text="3" /></div>
      <div class="carousel-item"><Placeholder style="width: 260px" class="bd-placeholder-img d-block h-auto ratio-1x1 rounded-5" color="#fff" background="#5c5c5c" text="4" /></div>
      <div class="carousel-item"><Placeholder style="width: 260px" class="bd-placeholder-img d-block h-auto ratio-1x1 rounded-5" color="#fff" background="#555" text="5" /></div>
      <div class="carousel-item"><Placeholder style="width: 260px" class="bd-placeholder-img d-block h-auto ratio-1x1 rounded-5" color="#fff" background="#4c4c4c" text="6" /></div>
      <div class="carousel-item"><Placeholder style="width: 260px" class="bd-placeholder-img d-block h-auto ratio-1x1 rounded-5" color="#fff" background="#444" text="7" /></div>
    </div>
  </div>`} />

Autoplay changed too, and the new behavior is stricter. It’s opt-in via `data-bs-autoplay="true"`, the old `ride` option is gone, and interacting with a carousel now stops autoplay permanently rather than resuming it. The new `.carousel-control-play-pause` button gives visitors a discoverable way to stop and resume playback.

<Example code={`<div id="carouselV6" class="carousel slide" data-bs-autoplay="true" style="--bs-carousel-items: 3; --bs-carousel-items-gap: 1rem; --bs-carousel-items-peek: 2rem;">
  <div class="carousel-inner">
    <div class="carousel-item active"><Placeholder class="d-block h-auto ratio-1x1 rounded-5" color="#fff" background="#777" text="1" /></div>
    <div class="carousel-item"><Placeholder class="d-block h-auto ratio-1x1 rounded-5" color="#fff" background="#6c6c6c" text="2" /></div>
    <div class="carousel-item"><Placeholder class="d-block h-auto ratio-1x1 rounded-5" color="#fff" background="#666" text="3" /></div>
    <div class="carousel-item"><Placeholder class="d-block h-auto ratio-1x1 rounded-5" color="#fff" background="#5c5c5c" text="4" /></div>
    <div class="carousel-item"><Placeholder class="d-block h-auto ratio-1x1 rounded-5" color="#fff" background="#555" text="5" /></div>
    <div class="carousel-item"><Placeholder class="d-block h-auto ratio-1x1 rounded-5" color="#fff" background="#4c4c4c" text="6" /></div>
  </div>
  <div class="d-flex justify-content-between align-items-center mt-3">
    <div class="carousel-indicators m-0">
      <button type="button" data-bs-target="#carouselV6" data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
      <button type="button" data-bs-target="#carouselV6" data-bs-slide-to="1" aria-label="Slide 2"></button>
      <button type="button" data-bs-target="#carouselV6" data-bs-slide-to="2" aria-label="Slide 3"></button>
      <button type="button" data-bs-target="#carouselV6" data-bs-slide-to="3" aria-label="Slide 4"></button>
      <button type="button" data-bs-target="#carouselV6" data-bs-slide-to="4" aria-label="Slide 5"></button>
      <button type="button" data-bs-target="#carouselV6" data-bs-slide-to="5" aria-label="Slide 6"></button>
    </div>
    <div class="d-flex gap-1">
      <button class="btn-icon btn-sm carousel-control-play-pause" type="button" data-bs-target="#carouselV6" aria-label="Pause" data-bs-pause-label="Pause" data-bs-play-label="Play">
        <span class="carousel-icon-pause" aria-hidden="true"></span>
        <span class="carousel-icon-play" aria-hidden="true"></span>
      </button>
      <button class="btn-icon btn-sm" type="button" data-bs-target="#carouselV6" data-bs-slide="prev" style="--bs-carousel-control-icon-width: 1rem;">
        <span class="carousel-icon-prev" aria-hidden="true"></span>
        <span class="visually-hidden">Previous</span>
      </button>
      <button class="btn-icon btn-sm" type="button" data-bs-target="#carouselV6" data-bs-slide="next" style="--bs-carousel-control-icon-width: 1rem;">
        <span class="carousel-icon-next" aria-hidden="true"></span>
        <span class="visually-hidden">Next</span>
      </button>
    </div>
  </div>
</div>`} />

## Dialog and Drawer

Dialog and Drawer are native `<dialog>` elements now, so the browser supplies the backdrop, the focus trap, and the inert top layer. Both components utilize some shared JavaScript code and shared base styles. Dialog also uses the native `showModal()` for true modal behavior.

Compared to v5, visually the header, body, and footer are the same chrome Modal had, just renamed and buttoned up a bit. Markup has been streamlined as a result of the native `<dialog>` element. Style-wise, the most notable change will be the blurred `::backdrop` for added contrast.

<Example full toggleMarkup class="bd-example-dialog" code={`<dialog class="dialog" open>
  <div class="dialog-header">
    <h1 class="dialog-title">Dialog title</h1>
    <button type="button" class="btn-close" aria-label="Close"></button>
  </div>
  <div class="dialog-body">
    <p>This is a native dialog element. It uses the browser’s built-in modal behavior for accessibility and focus management.</p>
  </div>
  <div class="dialog-footer">
    <button type="button" class="btn-solid theme-secondary">Close</button>
    <button type="button" class="btn-solid theme-primary">Done</button>
  </div>
</dialog>`} />

Dialogs come with more options via classes now:

- Transition the dialog with a slide up or slide down effect with the `.dialog-slide-down` or `.dialog-slide-up` modifier classes.
- Switch between the default modal behavior (where the background is non-interactive) to non-modal behavior with `data-bs-modal="false"` on the trigger button.
- Make a fullscreen dialog with the `.dialog-fullscreen` modifier class

All of that is shown in the examples below.

<Example full toggleMarkup code={`<div class="d-flex flex-wrap justify-content-center gap-2">
    <button type="button" class="btn-solid theme-primary" data-bs-toggle="dialog" data-bs-target="#defaultDialog">
      Default
    </button>
    <div class="btn-group btn-group-divider">
      <button type="button" class="btn-solid theme-primary" data-bs-toggle="dialog" data-bs-target="#slideDownDialog">
        Slide down
      </button>
      <button type="button" class="btn-solid theme-primary" data-bs-toggle="dialog" data-bs-target="#slideUpDialog">
        Slide up
      </button>
    </div>
    <div class="btn-group btn-group-divider">
      <button type="button" class="btn-solid theme-primary" data-bs-toggle="dialog" data-bs-target="#modalDialog">
        Modal
      </button>
      <button type="button" class="btn-solid theme-primary" data-bs-toggle="dialog" data-bs-target="#nonModalDialog" data-bs-modal="false"><!--[!code highlight]-->
        Non-modal
      </button>
    </div>
    <button type="button" class="btn-solid theme-primary" data-bs-toggle="dialog" data-bs-target="#fullscreenDialog">
      Fullscreen
    </button>
  </div>

  <dialog class="dialog" id="defaultDialog">
    <div class="dialog-header">
      <h1 class="dialog-title">Default dialog</h1>
      <button type="button" class="btn-close" data-bs-dismiss="dialog" aria-label="Close"></button>
    </div>
    <div class="dialog-body">
      <p>This is a default dialog.</p>
    </div>
    <div class="dialog-footer">
      <button type="button" class="btn-solid theme-secondary" data-bs-dismiss="dialog">Close</button>
      <button type="button" class="btn-solid theme-primary">Done</button>
    </div>
  </dialog>

  <dialog class="dialog dialog-slide-down" id="slideDownDialog"><!--[!code highlight]-->
    <div class="dialog-header">
      <h1 class="dialog-title">Slide down</h1>
      <button type="button" class="btn-close" data-bs-dismiss="dialog" aria-label="Close"></button>
    </div>
    <div class="dialog-body">
      <p>This dialog slides down from the top of the viewport.</p>
    </div>
    <div class="dialog-footer">
      <button type="button" class="btn-solid theme-secondary" data-bs-dismiss="dialog">Close</button>
      <button type="button" class="btn-solid theme-primary">Done</button>
    </div>
  </dialog>

  <dialog class="dialog dialog-slide-up" id="slideUpDialog"><!--[!code highlight]-->
    <div class="dialog-header">
      <h1 class="dialog-title">Slide up</h1>
      <button type="button" class="btn-close" data-bs-dismiss="dialog" aria-label="Close"></button>
    </div>
    <div class="dialog-body">
      <p>This dialog slides up from the bottom of the viewport.</p>
    </div>
    <div class="dialog-footer">
      <button type="button" class="btn-solid theme-secondary" data-bs-dismiss="dialog">Close</button>
      <button type="button" class="btn-solid theme-primary">Done</button>
    </div>
  </dialog>

  <dialog class="dialog" id="modalDialog">
    <div class="dialog-header">
      <h1 class="dialog-title">Modal dialog</h1>
      <button type="button" class="btn-close" data-bs-dismiss="dialog" aria-label="Close"></button>
    </div>
    <div class="dialog-body">
      <p>This uses <code>showModal()</code>. The page behind it is inert, focus is trapped, and the backdrop is native <code>::backdrop</code>.</p>
    </div>
    <div class="dialog-footer">
      <button type="button" class="btn-solid theme-secondary" data-bs-dismiss="dialog">Close</button>
    </div>
  </dialog>

  <dialog class="dialog" id="nonModalDialog">
    <div class="dialog-header">
      <h1 class="dialog-title">Non-modal dialog</h1>
      <button type="button" class="btn-close" data-bs-dismiss="dialog" aria-label="Close"></button>
    </div>
    <div class="dialog-body">
      <p>This dialog doesn’t block the page. You can still interact with content behind it.</p>
    </div>
    <div class="dialog-footer">
      <button type="button" class="btn-solid theme-secondary" data-bs-dismiss="dialog">Close</button>
    </div>
  </dialog>

  <dialog class="dialog dialog-fullscreen" id="fullscreenDialog"><!--[!code highlight]-->
    <div class="dialog-header">
      <h1 class="dialog-title">Fullscreen dialog</h1>
      <button type="button" class="btn-close" data-bs-dismiss="dialog" aria-label="Close"></button>
    </div>
    <div class="dialog-body">
      <p>This dialog covers the entire viewport.</p>
    </div>
    <div class="dialog-footer">
      <button type="button" class="btn-solid theme-secondary" data-bs-dismiss="dialog">Close</button>
    </div>
  </dialog>`} />

Drawer shares its show and hide lifecycle with Dialog through a common `DialogBase`, so it's a `<dialog>` too—just anchored to an edge. Shown here the way the docs do it: `open` and `.show` on the element, sitting in the page instead of the top layer.

<Example full toggleMarkup class="bd-example-drawer p-0 bg-1 overflow-hidden" code={`<dialog class="drawer drawer-start show" open tabindex="-1" id="drawerStatic" aria-labelledby="drawerStaticLabel">
  <div class="drawer-header">
    <h5 class="drawer-title" id="drawerStaticLabel">Drawer</h5>
    <button type="button" class="btn-close" aria-label="Close"></button>
  </div>
  <div class="drawer-body">
    Content for the drawer goes here. You can place just about any Bootstrap component or custom elements here.
  </div>
</dialog>`} />

Drawers behave just like Dialogs, but they have a different placement, transition, and backdrop styling. They also come with unique modifiers for side placements, as well as sheet-style bottom positioning.

<Example full toggleMarkup code={`<div class="d-flex flex-wrap justify-content-center gap-2">
  <button class="btn-solid theme-primary" type="button" data-bs-toggle="drawer" data-bs-target="#drawerExample" aria-controls="drawerExample">
    Default
  </button>
  <button class="btn-solid theme-primary" type="button" data-bs-toggle="drawer" data-bs-target="#drawerTranslucent" aria-controls="drawerTranslucent">
    Translucent
  </button>
  <button class="btn-solid theme-primary" type="button" data-bs-toggle="drawer" data-bs-target="#drawerSheet" aria-controls="drawerSheet">
    Sheet
  </button>
</div>

<dialog class="drawer drawer-start" tabindex="-1" id="drawerExample" aria-labelledby="drawerExampleLabel">
  <div class="drawer-header">
    <h5 class="drawer-title" id="drawerExampleLabel">Drawer</h5>
    <button type="button" class="btn-close" data-bs-dismiss="drawer" aria-label="Close"></button>
  </div>
  <div class="drawer-body">
    Some text as placeholder. In real life you can have the elements you have chosen. Like, text, images, lists, etc.
  </div>
</dialog>

<dialog class="drawer drawer-start drawer-translucent" tabindex="-1" id="drawerTranslucent" aria-labelledby="drawerTranslucentLabel"><!--[!code highlight]-->
  <div class="drawer-header">
    <h5 class="drawer-title" id="drawerTranslucentLabel">Translucent drawer</h5>
    <button type="button" class="btn-close" data-bs-dismiss="drawer" aria-label="Close"></button>
  </div>
  <div class="drawer-body">
    Content for the translucent drawer goes here. The background blurs and saturates whatever is behind it.
  </div>
</dialog>

<dialog class="drawer drawer-bottom drawer-sheet" tabindex="-1" id="drawerSheet" aria-labelledby="drawerSheetLabel"><!--[!code highlight]-->
  <div class="drawer-header">
    <h5 class="drawer-title" id="drawerSheetLabel">Sheet drawer</h5>
    <button type="button" class="btn-close" data-bs-dismiss="drawer" aria-label="Close"></button>
  </div>
  <div class="drawer-body">
    Content for the sheet drawer goes here. It sits flush to the edge with no inset, border, rounded corners, or shadow.
  </div>
</dialog>`} />

## Menu

[Menus](https://getbootstrap.com/docs/6.0/components/menu/), previously called Dropdowns, have a slew of new styles and support for various new types of content within menu items. You can now add icons, descriptions, and your own checkmark icons for selected states to menu items, as well as nested submenus.

Like Tooltips and Popovers, Menus now position with Floating UI instead of the deprecated Popper.js. Also, menus no longer are statically positioned in navbars; they now continue to use dynamic JavaScript positioning.

<Example full toggleMarkup class="d-flex flex-wrap gap-5 py-7 align-items-center justify-content-center" code={`<div class="menu show position-static">
  <button class="menu-item" type="button">
    <svg class="menu-item-icon fg-3" width="16" height="16" fill="currentColor" aria-hidden="true"><use href="#copy"/></svg>
    Copy
  </button>
  <button class="menu-item" type="button">
    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="menu-item-icon fg-3" viewBox="0 0 16 16">
      <path d="M3.5 3.5c-.614-.884-.074-1.962.858-2.5L8 7.226 11.642 1c.932.538 1.472 1.616.858 2.5L8.81 8.61l1.556 2.661a2.5 2.5 0 1 1-.794.637L8 9.73l-1.572 2.177a2.5 2.5 0 1 1-.794-.637L7.19 8.61zm2.5 10a1.5 1.5 0 1 0-3 0 1.5 1.5 0 0 0 3 0m7 0a1.5 1.5 0 1 0-3 0 1.5 1.5 0 0 0 3 0"/>
    </svg>
    Cut
  </button>
  <button class="menu-item" type="button">
    <svg class="menu-item-icon fg-3" width="16" height="16" fill="currentColor" aria-hidden="true"><use href="#clipboard"/></svg>
    Paste
  </button>
  <button class="menu-item" type="button" disabled>
    <svg class="menu-item-icon fg-3" width="16" height="16" fill="currentColor" aria-hidden="true"><use href="#archive"/></svg>
    Archive
  </button>
  <hr class="menu-divider">
  <button class="menu-item theme-danger" type="button">
    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="menu-item-icon" viewBox="0 0 16 16">
      <path d="M6.5 1h3a.5.5 0 0 1 .5.5v1H6v-1a.5.5 0 0 1 .5-.5M11 2.5v-1A1.5 1.5 0 0 0 9.5 0h-3A1.5 1.5 0 0 0 5 1.5v1H1.5a.5.5 0 0 0 0 1h.538l.853 10.66A2 2 0 0 0 4.885 16h6.23a2 2 0 0 0 1.994-1.84l.853-10.66h.538a.5.5 0 0 0 0-1zm1.958 1-.846 10.58a1 1 0 0 1-.997.92h-6.23a1 1 0 0 1-.997-.92L3.042 3.5zm-7.487 1a.5.5 0 0 1 .528.47l.5 8.5a.5.5 0 0 1-.998.06L5 5.03a.5.5 0 0 1 .47-.53Zm5.058 0a.5.5 0 0 1 .47.53l-.5 8.5a.5.5 0 1 1-.998-.06l.5-8.5a.5.5 0 0 1 .528-.47M8 4.5a.5.5 0 0 1 .5.5v8.5a.5.5 0 0 1-1 0V5a.5.5 0 0 1 .5-.5"/>
    </svg>
    Delete
  </button>
</div>

<div class="menu show position-static">
  <button class="menu-item" type="button">
    <span class="menu-item-content">
      Publish now
      <small class="menu-item-description">Make this page visible immediately</small>
    </span>
  </button>
  <button class="menu-item" type="button">
    <span class="menu-item-content">
      Schedule
      <small class="menu-item-description">Set a future publish date</small>
    </span>
  </button>
  <button class="menu-item" type="button">
    <span class="menu-item-content">
      Save as draft
      <small class="menu-item-description">Park this one for later</small>
    </span>
  </button>
  <hr class="menu-divider">
  <button class="menu-item theme-danger" type="button">
    Delete…
  </button>
</div>

<div class="menu show position-static">
  <button class="menu-item selected" type="button">
    <div class="menu-item-content">
      Selected menu item
      <small class="menu-item-description">Currently visible to users</small>
    </div>
    <svg class="menu-item-check" xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" viewBox="0 0 16 16"><path d="m2 7 4 5 8-8"/></svg>
  </button>
  <button class="menu-item" type="button">
    <div class="menu-item-content">
      Default menu item
      <small class="menu-item-description">Not yet published</small>
    </div>
    <svg class="menu-item-check" xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" viewBox="0 0 16 16"><path d="m2 7 4 5 8-8"/></svg>
  </button>
  <button class="menu-item" type="button" disabled>
    <div class="menu-item-content">
      Disabled menu item
      <small class="menu-item-description">This item is disabled</small>
    </div>
    <svg class="menu-item-check" xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" viewBox="0 0 16 16"><path d="m2 7 4 5 8-8"/></svg>
  </button>
</div>`} />

Positioning for menus has also been updated. We’ve removed directional modifier classes—`.dropstart`, `.dropend`, and `.dropup`. Admittedly, this was a strange approach to begin with. Instead, use `data-bs-placement` to control direction instead (for example, `.dropup` becomes `data-bs-placement="top"`).

Big news for Menus, we now have [submenu support](https://getbootstrap.com/docs/6.0/components/menu/#submenus). Add nested menus—no need for additional data attributes, just wrap a `.menu-item` in a `.submenu`, then add another `.menu` inside.

<Example full toggleMarkup class="d-flex justify-content-center overflow-visible" code={`<button class="btn-solid theme-secondary" type="button" data-bs-toggle="menu" aria-expanded="false">
    With submenus
  </button>
  <div class="menu">
    <div class="submenu">
      <button class="menu-item" type="button">File</button>
      <div class="menu">
        <a class="menu-item" href="#">New</a>
        <a class="menu-item" href="#">Open</a>
        <a class="menu-item" href="#">Save</a>
      </div>
    </div>
    <div class="submenu">
      <button class="menu-item" type="button">Edit</button>
      <div class="menu">
        <a class="menu-item" href="#">Cut</a>
        <a class="menu-item" href="#">Copy</a>
        <a class="menu-item" href="#">Paste</a>
      </div>
    </div>
    <div class="submenu">
      <button class="menu-item" type="button">View</button>
      <div class="menu">
        <a class="menu-item" href="#">Zoom In</a>
        <a class="menu-item" href="#">Zoom Out</a>
      </div>
    </div>
    <hr class="menu-divider">
    <a class="menu-item" href="#">Preferences</a>
  </div>`} />

[Head to the docs](https://getbootstrap.com/docs/6.0/components/menu/) to learn more about all the options our new Menus offer.

## Navbar

[Navbars](https://getbootstrap.com/docs/6.0/components/navbar/) now use a Drawer-based responsive navigation instead of the old collapse. Menus in navbars no longer get static positioning.

<ResizableExample full toggleMarkup code={`<nav class="navbar md:navbar-expand bg-1">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Navbar</a>
    <button class="btn-icon navbar-toggler" type="button" data-bs-toggle="drawer" data-bs-target="#navbarDrawer" aria-controls="navbarDrawer" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon" aria-hidden="true"></span>
    </button>
    <dialog class="drawer drawer-end" tabindex="-1" id="navbarDrawer" aria-labelledby="navbarDrawerLabel">
      <div class="drawer-header">
        <h5 class="drawer-title" id="navbarDrawerLabel">Menu</h5>
        <button type="button" class="btn-close" data-bs-dismiss="drawer" aria-label="Close"></button>
      </div>
      <div class="drawer-body mb-2 md:mb-0">
        <ul class="nav navbar-nav me-auto">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="#">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">Link</a>
          </li>
          <li class="nav-item">
            <button class="nav-link" type="button" data-bs-toggle="menu" aria-expanded="false">
              Menu
            </button>
            <div class="menu">
              <a class="menu-item" href="#">Action</a>
              <a class="menu-item" href="#">Another action</a>
              <hr class="menu-divider">
              <a class="menu-item" href="#">Something else here</a>
            </div>
          </li>
          <li class="nav-item">
            <a class="nav-link disabled" aria-disabled="true">Disabled</a>
          </li>
        </ul>
        <form class="hstack gap-2" role="search">
          <input class="form-control" type="search" placeholder="Search" aria-label="Search"/>
          <button class="btn-solid btn-icon theme-secondary" type="submit">
            <svg class="bi" width="16" height="16"><use href="#search" /></svg>
          </button>
        </form>
      </div>
    </dialog>
  </div>
</nav>`} />

## Nav overflow

[Nav overflow](https://getbootstrap.com/docs/6.0/components/nav-overflow/) is a new component that has been a repeated request from folks using Bootstrap nav menus. They’re built with container queries and powered by a new JavaScript plugin. Wrap a `.nav` in `.nav-overflow` with a `data-bs-toggle="nav-overflow"` attribute and watch as menus that expand beyond the parent's width are hidden automatically into a Menu.

<ResizableExample full toggleMarkup code={`<div class="nav-overflow" data-bs-toggle="nav-overflow">
  <ul class="nav nav-pills">
    <li class="nav-item">
      <a class="nav-link active" aria-current="page" href="#">Home</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="#">Dashboard</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="#">Products</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="#">Services</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="#">Analytics</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="#">Reports</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="#">Settings</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="#">Help</a>
    </li>
  </ul>
</div>`} />

Navigation components have been tweaked a bit. The base `.nav` component has better state support (active, hover, and disabled). Pills have a new look to differentiate them better from the updated default nav option.

<Example full toggleMarkup class="d-flex justify-content-center" code={`<ul class="nav">
  <li class="nav-item">
    <a class="nav-link active" aria-current="page" href="#">Active</a>
  </li>
  <li class="nav-item">
    <a class="nav-link" href="#">Link</a>
  </li>
  <li class="nav-item">
    <a class="nav-link" href="#">Link</a>
  </li>
  <li class="nav-item">
    <a class="nav-link disabled" aria-disabled="true">Disabled</a>
  </li>
</ul>`} />

Nav components can also be themed using the `.theme-<color>` helper classes.

<Example full toggleMarkup class="d-flex flex-wrap gap-5 align-items-center justify-content-center" code={`<ul class="nav theme-accent">
    <li class="nav-item">
      <a class="nav-link active" aria-current="page" href="#">Active</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="#">Link</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="#">Link</a>
    </li>
    <li class="nav-item">
      <a class="nav-link disabled" aria-disabled="true">Disabled</a>
    </li>
  </ul>

  <ul class="nav nav-pills theme-accent">
    <li class="nav-item">
      <a class="nav-link active" aria-current="page" href="#">Active</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="#">Link</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="#">Link</a>
    </li>
    <li class="nav-item">
      <a class="nav-link disabled" aria-disabled="true">Disabled</a>
    </li>
  </ul>`} />

## Accordion

[Accordions](https://getbootstrap.com/docs/6.0/components/accordion/) have been rebuilt on the native `<details>` and `<summary>` elements, meaning they no longer require any JavaScript. They've also been given a visual refresh, theme support, a small variant, and more.

<Example full toggleMarkup code={`<div class="accordion accordion-sm">
  <details class="accordion-item" name="accordionExample" open>
    <summary class="accordion-header">
      Accordion Item #1
      <svg class="accordion-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"><path d="m2 5 6 6 6-6"/></svg>
    </summary>
    <div class="accordion-body">
      <strong>This is the first item’s accordion body.</strong> It is shown by default because the <code>open</code> attribute is present. The native <code>&lt;details&gt;</code> element handles all the show/hide logic without any JavaScript. You can put any HTML content within the <code>.accordion-body</code>.
    </div>
  </details>
  <details class="accordion-item" name="accordionExample">
    <summary class="accordion-header">
      Accordion Item #2
      <svg class="accordion-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"><path d="m2 5 6 6 6-6"/></svg>
    </summary>
    <div class="accordion-body">
      <strong>This is the second item’s accordion body.</strong> The <code>name</code> attribute groups this with other accordion items. When you open this item, any other open item in the same group will close automatically.
    </div>
  </details>
  <details class="accordion-item" name="accordionExample">
    <summary class="accordion-header">
      Accordion Item #3
      <svg class="accordion-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"><path d="m2 5 6 6 6-6"/></svg>
    </summary>
    <div class="accordion-body">
      <strong>This is the third item’s accordion body.</strong> This exclusive accordion behavior is built into the browser—no Bootstrap JavaScript required. Just use matching <code>name</code> attributes on your <code>&lt;details&gt;</code> elements.
    </div>
  </details>
</div>`} />

## Future of components

One of the things I'm most excited about downstream in v6's lifecycle is the potential for building web component versions of our components. Consider the complexities of certain components, from even a checkbox to…

---

Thanks for reading! This post is super long, and it still doesn’t cover all the component updates we made in v6. Please, please visit the [v6.0.0 migration guide](https://getbootstrap.com/docs/6.0/guides/migration/) to see a complete list of changes with context.

Stay tuned for the next post, too!.
